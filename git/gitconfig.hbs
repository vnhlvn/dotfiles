[includeIf "gitdir:~/work/"]
  path = ~/.gitconfig.work
[includeIf "gitdir:~/personal/"]
  path = ~/.gitconfig.personal
[gpg]
  format = ssh
[commit]
  gpgSign = true
[gpg "ssh"]
  program = "{{ gpgSshProgram }}"
